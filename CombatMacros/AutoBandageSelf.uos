while not dead
  if not timerexists 'Bandage Timer'
    createtimer 'Bandage Timer'
    settimer 'Bandage Timer' 14250
  endif
  if timer 'Bandage Timer' < 14250
    pause 100
  endif
  if poisoned 'self' or hits < maxhits
    bandageself
    settimer 'Bandage Timer' 0
  endif
endwhile